const initState = {
    tests: [],
}

const testReducer = (state = initState, action) => {
    switch (action.type){
        
        default: 
            return state;
    }
}

export default testReducer